import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

describe('first test', () => {
    it('My test case', () => {
        expect(true).toEqual(true);
    })
})
